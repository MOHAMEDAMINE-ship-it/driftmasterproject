{% extends 'base-back.html.twig' %}

{% block title %}Vihecule{% endblock %}

{% block body %}
    <h1>Vihecule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vihecule.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ vihecule.nom }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ vihecule.marque }}</td>
            </tr>
            <tr>
                <th>Puissance</th>
                <td>{{ vihecule.Puissance }}</td>
            </tr>
            <tr>
                <th>Consommation</th>
                <td>{{ vihecule.consommation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vihecule_index') }}">back to list</a>

    <a href="{{ path('vihecule_edit', {'id': vihecule.id}) }}">edit</a>

    {{ include('vihecule/_delete_form.html.twig') }}
{% endblock %}
